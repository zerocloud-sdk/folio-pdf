/**
 * Project-owned Capability Provider contract, deterministic selection,
 * bounded immutable request/result values, stable failures, and explicit
 * remote-disclosure policy. The default Workflow Environment exposes only
 * metadata from an empty offline catalog.
 */
package net.zerocloud.pdf.provider;
