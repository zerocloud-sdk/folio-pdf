# Publish a unified Release Train to Maven Central

All first-party modules share one version during `0.x` and are selected through `net.zerocloud:pdf-bom`. Formal releases are published to Maven Central with POM metadata, source and Javadoc artifacts, signatures, checksums, and the Apache-2.0 license; GitHub or other repositories may mirror artifacts but are not the canonical dependency source.
