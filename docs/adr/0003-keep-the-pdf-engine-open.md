# Keep the PDF engine open

No particular low-level PDF engine is part of the product contract. PDFBox may be used where it fits, but the implementation may instead extend it, bypass it, use other appropriately licensed dependencies, or implement capabilities directly, because the complete Reference Suite exceeds the scope of any single candidate backend.
