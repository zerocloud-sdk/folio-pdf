# T29 / #30 independent Spec coverage review

Final review updated: 2026-09-06T17:44:08.187994+00:00 (2026-09-07 Asia/Shanghai). Reviewer: clean-context independent Spec agent `/root/t29_final_spec`; not an implementer. All identified source findings F2/F3/F5/F6 and the local verification finding F4 are closed. Current snapshot `94b628481751378665a40a3b033430b1224ec0adc817f811386bc44db59ef24f` has six-chain Linux acceptance PASS, full root PASS, all four complete JDK gates PASS and inventory PASS. Root and each JDK have 1,019 tests, zero failures/errors and three existing optional scale skips. Source/artifact/image/log/clone/workspace linkage is independently checked. **F1 remains: Windows x86-64, macOS x86-64 and macOS arm64 actual execution receipts are absent and INDETERMINATE; #30 is unfinished.** No completion checkbox, whole-issue signoff or commit authorization is supplied. The top-level map and last follow-up give the final disposition; earlier chronological sections preserve their own point-in-time observations and pending statuses.

## Authority, scope and method

The controlling contract is `/home/ubuntu/.codex/attachments/529f9310-4823-4965-814a-c7ccefcd3b5b/pasted-text-1.txt` (line references below are `C:n`). I read its entire contents; the full cached bodies and comments of `/tmp/folio-t29/issue-30.json` and `issue-1.json`; AGENTS.md; CONTEXT.md; CONTRIBUTING.md; the issue-tracker/domain instructions; ADRs 0009, 0010, 0022–0026, 0033 and new 0039; the existing Provider, fonts, Unicode and Worker contracts; the T29 reference and delivery documents. The code-review skill's Spec axis applies. The user explicitly overrides its ordinary committed comparison.

The fixed baseline is `dd4cda28c24a304258e75f0854d86852275f5320`, which resolves and equals HEAD. `git log BASE..HEAD --oneline` is empty. The review inspected `git diff BASE --` and every relevant untracked delivery family from `git ls-files --others --exclude-standard`, including Java, C/Meson, Python, tests, binary reference resources, manifests, CI/Maven, inventory and documentation. Empty `BASE...HEAD` was not mistaken for an empty change. Repository paths below are relative to `/home/ubuntu/IdeaProjects/open-pdf`.

The reviewer ran no Maven command, native build, test generator, installer or product acceptance command, and made no repository edits, commit, push, PR, tracker write or publication. Read-only hash comparisons and `git diff --check BASE --` were performed. Existing receipts/logs were inspected, not relabeled as reviewer executions. The only reviewer-written artifact is this file.

## Findings and delivery limits

### F1 — Mandatory external platform evidence is missing (confirmed incomplete requirement)

Contract: C:60–62 requires each of Windows x86-64, macOS x86-64 and macOS arm64 to have “完整、可追溯的固定版本实测证据”; C:79–80 says absent tools/platforms “必须记为 INDETERMINATE，不能视为 #30 完成.”

`capabilities/evidence/T29-shaping.md:56`–65 explicitly records all three receipts as unavailable. `scripts/t29-native-observation.py:110`–111 only implements live engine attribution on Linux. `.github/workflows/ci.yml:14` uses Ubuntu; existing Linux tool assets and four Linux JDK runs cannot establish native/IN_PROCESS execution on the other platforms. This is an external evidence/implementation-path gap, not proof of a shaping defect on those platforms. All three determinations must remain INDETERMINATE and prevent whole-issue completion.

### F2 — Buffered relayout accumulated initial shaping reservations (resolved source defect)

Contract: C:72–73 requires stable bounded failures while “保持既有资源所有权及失败前不发布的合同.” Parent #1 requires explicit bounded memory and preservation of existing lifecycle behavior; `docs/harfbuzz-shaping.md:226`–240 distinguishes temporary candidate memory from retained paint plans.

At the initial reviewed source, `PdfBoxParagraphOperations.java:190` repeatedly composes with the retained `bufferedText`; `shapeAtoms` at 1010–1033 passes null ownership to `shapeRuns`. `PdfBoxPositionedTextOperations.java:174`/214 assigns those native decoded-result reservations to a PreparedText-wide `shapingMemory` scope, closed only at 268. `ShapingCoordinator.java:90` retains result memory there. Closing each Layout at paragraph line 943 only releases `shapingPlans`. Consequently repeated relayout charges discarded initial metrics cumulatively and can hit OWNED_MEMORY_LIMIT_EXCEEDED without corresponding live plans. `/tmp/folio-t29/shaping-relayout-memory-review-red.log` reproduces failure in both modes. The corrected source now passes explicit ownership from Layout.shapingPlans (`PdfBoxParagraphOperations.java:700`, 746–748) or the emission scope (`PdfBoxLargeTableOperations.java:182`, 195), through table preparation/atoms to `PreparedText.shape` (`PdfBoxPositionedTextOperations.java:211`–218 in the final source). The PreparedText-wide initial-result scope is removed. Each layout closes its scope after painting; table emission does the same. Public regression at `ShapingCompositionWorkflowTest.java:307` compares one versus 32 repeated relayouts within a bounded peak-memory delta and then forces isolated Arabic forms, reopening to verify 32 mappings/advances. The red log reproduces both-mode failure; mapping-green has two passes and refactor six passes including ownership regressions. The intermediate first green exposed an incorrect TextItem contribution assertion, subsequently corrected to CharacterMapping; it is not counted as a successful receipt. Source lifetime correction and regression reviewed. This describes modeled accounting retention, not an assertion of an unbounded Java heap leak.

### F3 — Shaped ActualText required a higher PDF version (resolved source defect)

Contract: C:37–38 requires work within the existing Workflow boundary; C:72–73 preserves stable failures/ownership/no publication; C:85 requires contracts and migration documentation to match implementation. Parent #1 makes standards the correctness authority and PDF version policy explicit.

The original `PdfBoxShapedFont.java:79` unconditionally emitted a Span marked-content `/ActualText` property, while publication version validation only covered supplementary mappings. Marked-content ActualText requires PDF 1.5 (structure-element ActualText has a different 1.4 floor): ISO 32000-1 §14.9.4 / [Adobe PDF 1.7 reference §10.8.3](https://opensource.adobe.com/dc-acrobat-sdk-docs/pdfstandards/pdfreference1.7old.pdf).

The reviewed correction at `PdfBoxPositionedTextOperations.java:145`–148 in the final source rejects shaped composition below PDF 1.5 before font staging; safe diagnostic at 1530–1532 and WorkerFailureCatalog entries preserve stable failure transport. Public test `ShapingCompositionWorkflowTest.java:241` uses a BMP-only project font with incremental PDF 1.2/1.4/1.5, checks both execution modes, unchanged source/target and NOT_ATTEMPTED receipts, and reopens the successful 1.5 result. The red log demonstrated incorrect publication in both modes; the first green exposed missing Worker diagnostic transport; worker-green and refactor logs now pass. The minimum is now synchronized in `docs/harfbuzz-shaping.md:210`–214, Chinese guide 179–181, matrix 1965 and PROVENANCE. Source correction, tests and documentation reviewed.

### F4 — Complete current-source local verification and archival (resolved)

Contract: C:76 requires “新增最小相关测试及受影响的字体、Unicode、段落/表格回归通过”; C:77 requires “./mvnw -B -ntp verify 通过”; C:78 requires “./scripts/verify-jdk-matrix.sh 在 JDK 8/11/17/21 全部通过”; C:86 requires “PROVENANCE、依赖声明和 Acceptance Evidence 与实际制品对应.” C:59 separately requires traceable fixed-version Linux execution and C:87 the current inventory check.

These local requirements are now satisfied on frozen current source snapshot `94b628481751378665a40a3b033430b1224ec0adc817f811386bc44db59ef24f`. Six-chain Linux acceptance finished at 16:38:23Z; full root verify at 16:57:03Z; complete JDK8/11/17/21 at 16:56:11Z/17:14:22Z/17:15:26Z/17:29:27Z. Root and every JDK independently total 58 class records, 1,019 tests, zero failures/errors and three skips solely in the existing optional Worker scale class. The exact `8 11`, `17` and `21` enclosing script receipts all record actual exit 0. Current inventory passes with generated documentation current. Aggregate `T29-verified-jdk-matrix.json` links exact raw logs, command receipts, actual Java versions, unchanged image IDs and source identity. All declared source and receipt/log hashes were independently checked, including all 663 inputs in the worktree and both verification clones after all gates.

The font-snapshot/accounting repair is covered by meaningful public Red/Green/refactor and these complete current-source gates, including the previously failing full T28 Worker trigger. Earlier JDK8 failures, diagnosis, historical source snapshot and Linux artifacts remain preserved and explicitly historical. No earlier pass, prototype-only success, altered resource default or relaxed profile substitutes for the current results. Current runtime JAR inspection, source/artifact/visual equivalence, inventory and final workspace/link checks also pass; detailed independent observations appear below. F4 is closed. This closes local verification only: F1's three mandatory nonLinux executions remain INDETERMINATE and prohibit whole-issue completion.

### F5 — Grapheme fallback undercounted scalar visits (resolved source defect)

Contract: C:72–73 preserves existing bounded failure/ownership contracts; C:102 forbids relaxing resource defaults to pass. `docs/font-loading.md:294` defines a fallback visit per input scalar, including successful coverage. The initial `fontForCluster` charged once per candidate font rather than once for each scalar inspected. The corrected `PdfBoxPositionedTextOperations.java:198`–202 in the final source checkpoints and charges before each scalar coverage lookup. Public regression `ShapingCompositionWorkflowTest.java:325` checks a two-scalar Devanagari grapheme with exact bounds: three total visits fail FONT_LIMIT_EXCEEDED with unchanged target/NOT_ATTEMPTED; four succeed and reopen correctly in both modes. The recorded red contains two erroneous-success failures; green has two passes. English docs at 141–142 state the unchanged counting rule. Full-suite refactor receipt now records 34 tests, zero failures/errors/skips and BUILD SUCCESS; final root/JDK/acceptance gates remain F4.

### F6 — Shared snapshots initially omitted per-declaration retained metadata (resolved source defect)

Contract: C:72–73 requires stable bounded failures while “保持既有资源所有权及失败前不发布的合同”; C:102 forbids “放宽资源默认值让验收通过.” The first canonicalization patch charged unique content bytes/key but retained an identity-map entry for every distinct borrowed FontSource without a separate metadata charge. Equal-content hits therefore hid retained per-declaration metadata from owned-memory accounting. This reviewer raised the issue; it was confirmed by a public observed-peak regression, not an internal map-size assertion.

The corrected `PdfBoxPositionedTextOperations.java:873`–875 reserves 128 modeled retained bytes before every first `stagedOneShotSources.put`; line 869 separately charges unique content/key storage. Repeated use of the same declaration creates no new identity entry or repeated charge. The public test at `FontLoadingWorkflowTest.java:338`–340 requires the distinct-declaration run to report a larger peak than the one-reused-declaration control while staying within a control-derived bounded policy. `/tmp/folio-t29/font-declaration-accounting-red.log` records the exact assertion failure before correction; green records one pass; the subsequent refactor records 22 FontLoading, 34 shaping and two T28 acceptance cases, all passing. Source correction and focused regression reviewed; F6 is closed. This is a modeled ownership charge, not a claim of exact JVM object size.

No unasked product feature, unsupported facade stub, unofficial Java wrapper, bundled native runtime, unrelated tracker/release action, or other confirmed source defect was found. The narrow font-snapshot repair is necessary regression work within C:76/C:78 and preserves the existing font/Workflow boundary. F2/F3/F5/F6 source repairs and F4 local verification have been independently inspected and closed. F1 remains the mandatory unresolved delivery/evidence requirement.

## Per-profile completion coverage (C:45–56)

All four profiles are independently fixed in `capabilities/profiles/T29-shaping-reference.md:11`–97 and `pdf-acceptance/src/main/resources/net/zerocloud/pdf/acceptance/shaping/T29-corpus.json`. Official HarfBuzz 10.2.0 hb-shape with OT functions/shaper, default features, explicit script/language/direction, UPEM scale and monotone grapheme clusters supplies all numeric expectations. The generator `scripts/t29-shaping-reference.py` invokes that independent executable and uses fontTools 4.59.2 with the earlier independent raw-PDF object writer; it does not call Folio, PDFBox, ICU or the project adapter.

`T29-glyphs.tsv` contains 99 glyph observations in 36 runs: Arabic lines 2–26 (25 glyphs), Hebrew 27–53 (27), Devanagari 54–76 (23), Thai 77–100 (24), nine native runs per script. Each row fixes the source font, run logical UTF-16 input, direction, GID, cluster interval, x/y advances, x/y offsets and PDF coordinates. References below abbreviate the full TSV as G and the native/semantic checks as N/S: `T29NativeShapingAssertions.java:43`–85 / `T29ShapingSemanticAssertions.java:49`–113. Source coverage and the earlier six-chain Linux PASS were independently verified against the original frozen input snapshot. That run is explicitly historical after the font-cache executable repair. Fresh source-linked acceptance on snapshot 94b62848… now also passes; all current PNGs are byte-identical to the earlier inspected images and both-mode PDFs differ only in trailer ID values. Full root/JDK gates now pass and F4 is closed. No Linux result establishes the absent platforms.

| Contract criterion | Arabic | Hebrew | Devanagari | Thai |
| --- | --- | --- | --- | --- |
| C:46 GIDs | G:2–26; line 1 [236,770,292,705,292,1163,1085,668,46], marked lam-alef [292,704]. N compares every GID/count. | G:27–53; line 1 [23,46,124,55,79,100,96], marked shin [79,100,96]. N exact. | G:54–76; conjunct/matra [610,179], marked syllable [619,25,652], inserted dotted circle 134. N exact. | G:77–100; sara-am [71,59,49,86], stacked marks [29,92,49]. N exact. |
| C:47 clusters/input mappings | Descending RTL intervals; [292,704] share [0,3); multi-mark line 1 ranges [8,10),[5,8),[2,5),[1,2),[0,1). N validates intervals; S validates every full-cluster ToUnicode. | Shin/marks share [0,3), holam/vav [4,6); all other ranges exact. N/S. | Conjunct [0,4), syllable [0,3), fallback space+mark [1,3), including inserted GID134. N/S. | Both first probes share [0,3), fallback marked space [1,3). N/S. |
| C:48 advances | E.g. lam-alef 582, mark 0 source units; every x/y advance exact, PDF x advances scaled by .012. | Shin 730, marks 0; every x/y advance exact. | Matra/conjunct 259/712; marked syllable 259/762/0. All exact. | Sara-am 613/0/0/406; stacked cluster 600/0/0. All exact. |
| C:49 offsets | Marked lam-alef mark (249,256); line-1 marks include (89,-76),(260,256),(249,74),(257,-126). N exact; S position tolerance .0001 pt. | Shin marks (227,0),(539,0); vav/holam include (82,0),(-10,0). N/S. | All declared offsets exact (zero in these probes); reordering is in GID sequence and nonzero advances. N/S. | Tone (-29,0); stacked marks (-2,0),(-2,-59). N/S. |
| C:50 direction | Arab/ar RTL; mixed Latin runs LTR, explicit visual run order. N compares native enum; S sequence/positions and negative direction control. | Hebr/he RTL; mixed Latin LTR. Same checks. | Deva/hi LTR; mixed Latin LTR. Same checks. | Thai/th LTR; mixed Latin LTR. Same checks. |
| C:51 ligatures | Lam-alef substitution and marked shared cluster exact; emergency wrap reshapes at original graphemes. | Exact default-feature glyph sequence matches this profile; no unrelated ligature coverage claimed. | Conjunct substitution exact, including source glyph without an independent cmap mapping. | Exact default-feature sequence matches this profile; no unrelated ligature coverage claimed. |
| C:52 reordering | Native RTL glyph order preserved inside each run; ICU reorders run/cluster placement, not individual native glyphs. N/S. | RTL and multiple mark order exact. N/S. | Pre-base matra GID610/619 before base/conjunct despite logical order. N/S. | Sara-am decomposition/reordering exact. N/S. |
| C:53 combining marks | Shared clusters, zero advances, positive/negative x/y mark placement. N/S and offset negative control. | Multiple marks share base, independent offsets. N/S. | Anusvara/marks and inserted dotted circle preserved. N/S. | Stacked vowel/tone positions and decomposition. N/S. |
| C:54 explicit fallback | Noto Sans first, Noto Sans Arabic second; Latin/unmarked space first; `ا + space + fatha` keeps space/mark together in script font. | Same declaration order; `ש + space + qamats` whole marked space selects Hebrew. | Same order; `क + space + matra` selects Devanagari and emits dotted circle. | Same order; `ก + space + vowel` selects Thai. |
| C:55 reopened embedded subsets | S observes embedded subset Type0 resources; qpdf/fontTools verifier follows actually painted CIDs to embedded GIDs and compares Arabic source outlines/components. | Same source/font hash and outline verification for Hebrew. | Same; negative controls omit GID610 or transitive component607 of composite619 and must fail after reopening. | Same actual glyph-program verification for Thai. |
| C:56 pagination/geometry/rendering | Pages1–2; three lines/page, explicit12pt narrow width; two frozen visual property files. | Pages3–4; 12pt narrow width; two properties. | Pages5–6; 16pt narrow width; two properties. | Pages7–8; 12pt narrow width; two properties. |

For every C:56 cell: 240×192 pt pages, absent CropBox, 24 pt margins, 12 pt text, 48 pt leading; eight pages and exact line/run membership. S checks effective page bounds, all text matrices/advances and .0001-point positions, and observed logical-run replacement text. Native font-unit tolerance is zero. Visual profile files fix PDFium v0.11.2/chromium-7881, ImageMagick 7.1.2-30, 144 DPI, 480×384, opaque-white 8-bit sRGB, zero-fuzz primary AE **0**, and secondary disagreement ≤3000 pixels. Separate qpdf 12.4.0 syntax checks do not masquerade as standards validation. No tolerance was lowered or profile dropped.

`scripts/t29-verify-subsets.py` reopens through pinned qpdf JSON, follows the painted CIDToGIDMap, compares full glyph outline flags/coordinates and recursively transformed composite dependencies to the pinned original font, and checks cluster ToUnicode where present. This goes beyond codepoint coverage or mere subset names. Public semantic checks additionally require explicit mappings in product PDFs. Read-only auditing matched all reference artifact, font, generator and eight raster hashes to their receipts/profiles.

## Remaining completion criteria (C:59–92)

| Contract line / exact requirement | Coverage and determination |
| --- | --- |
| 59 Linux complete traceable fixed execution | COVERED on current snapshot 94b62848…: six-chain PASS at 16:38:23Z with independently verified source/artifact/native installation linkage. Full root and all four JDK gates also pass. F4 is closed; Linux evidence does not establish any missing external platform. |
| 60 Windows x86-64 | F1: INDETERMINATE. Native/IN_PROCESS execution, tools and complete receipts absent. Worker is outside existing OS support. |
| 61 macOS x86-64 | F1: INDETERMINATE. Same missing complete execution evidence; no Linux substitution. |
| 62 macOS arm64 | F1: INDETERMINATE. Same missing complete execution evidence; no cross-compilation substitution. |
| 63 project-owned adapter, no unofficial wrapper | Project-authored C helper/Meson and HarfBuzzCapabilityProvider, detached provider-contract DTOs; no wrapper dependency or document→conversion production cycle. Supplementary read-only inspection of all 744 entries in five current runtime JARs found no known unofficial wrapper packages; this supplements the full source/dependency review rather than treating a name scan as exhaustive. |
| 64 explicit engine install, no default/product native bundle | Explicit offline installer/helper selection; Maven does not execute native installation or package an engine. Five current runtime JARs hash-match the post-root receipt; independent ELF/PE/Mach-O/name/suffix scan of all 744 entries finds no native engine/helper payload. Product/Worker metadata remain separate from the external installation. |
| 65 dependency/tool/font version/source/hash/license | `scripts/harfbuzz-pin.properties`; third-party HarfBuzz source/COPYING and oracle/product receipts; DEPENDENCIES.md; font manifest/README/sources.json; pinned fontTools/PDFium/qpdf/ImageMagick. Actual installed tool binaries/linker/compiler options and archive/license identities are recorded. NonLinux actual toolchain receipts remain F1. |
| 68–69 Workflow/reopening primary; real native Provider tests | `ShapingCompositionWorkflowTest` parameterized both modes and T29 products/semantic command assert public commands, publication, reopened queries and public PdfValues. `HarfBuzzCapabilityProviderTest` uses actual externally installed helper through ProviderRequest/Result, not a fake adapter or internal mocking seam. |
| 70–71 same public contract in supported two modes | Current public inputs and bounded parent brokerage retain the existing Worker envelope. Current focused tests, full root and each JDK gate, and source-linked semantic/syntax/subset/visual acceptance cover both modes. Worker remains Linux/JDK8/11/17/21 only; mandatory other-platform native/IN_PROCESS evidence remains F1. |
| 72–73 stable absent/mismatch/malformed/limit failures, ownership/no publication | Real external Provider/public Workflow tests cover the required stable failures, NOT_ATTEMPTED/sentinel outputs, temporary cleanup, borrowed streams/channels, unshaped behavior, relayout and table lifecycle. F2/F3/F5/F6 are closed. Snapshot sharing preserves full-content equality, per-occurrence limits, declaration accounting and read-once borrowed ownership. Current full root and all four JDK gates pass the affected cases. |
| 74–75 five effective negative controls, no internal coupling | `T29ShapingEvidenceCommandTest.java` mutates public published PdfValues/DocumentPatch for wrong cluster, reversed direction, 1pt offset, wrong script font resource and missing painted glyph; reopened semantic assertions reject all five. FontTools verifier separately rejects missing shape/component. Tests assert behavior/errors/resources, not PDFBox object identity/private invocation. |
| 76 focused and affected regression tests pass | COVERED by strict meaningful Red/Green/refactor development, the latest 58-case focused repair suite and current complete root/JDK8/11/17/21 gates. Each full gate has 1,019 tests with zero failures/errors, including all relevant native/font/shaping/Unicode/paragraph/table/acceptance classes. Only the three unchanged optional Worker scale tests skip. |
| 77 root `./mvnw -B -ntp verify` | CURRENT PASS: actual exit 0, BUILD SUCCESS at 2026-09-06T16:57:03Z, 16:38 minutes, 1,019/0/0/3 on snapshot 94b62848…. Independently verified archived/original log identity and hash, all 58 per-class totals, environment receipt and unchanged 663 source inputs after build. Details and log hash below. |
| 78 `./scripts/verify-jdk-matrix.sh` JDK8/11/17/21 | CURRENT PASS: complete JDK8/11/17/21 each has 58 class records and 1,019/0/0/3 on 94b62848…. Actual enclosing commands `8 11`, `17`, `21` all exit 0. Aggregate T29-verified-jdk-matrix.json links exact command/raw-log/source/version/image receipts; independent hashes and totals agree. Final gate completed 17:29:27Z; F4 closed. This is Linux execution only. |
| 79–80 complete T29 evidence; missing INDETERMINATE | UNMET solely for F1 mandatory nonLinux execution evidence. Local six-chain T29 recorder/public tests/root/four-JDK gates are complete; the recorder has its own canonical and dedicated reactor paths and does not substitute T28 evidence. Missing three platforms remain INDETERMINATE and #30 unfinished. Matrix compatibility promotion remains separately gated. |
| 83 matrix follows actual evidence/dependencies | `capability-matrix.yaml:1936`–2056 stays experimental, enumerates public APIs/limitations, compatible dependency and T29/T06 promotion gates. Lines 2030–2051 now link actual Linux syntax/semantic/visual PASS records; certified-platforms remains empty. Six-chain hub links retain other observations without treating them as standards or platform certification. |
| 84 facade mapping/exclusion, no stable stub | `facade-surface.yaml:199`–201 explicitly excludes T29 because existing Preview layout.Document close-ownership mapping does not express this contract; stable/preview shaping arrays empty. No facade source stub. |
| 85 English/Javadoc/migration/Chinese consistent | English/Chinese/ADR/Provider/font/Unicode/table/Worker contracts cover explicit opt-in, migration, PDF1.5 floor, private snapshot sharing and per-declaration accounting. Seventeen getter comments passed strict zero-warning Javadoc; their intermediate debug-only class delta is disclosed and included in the fully tested current snapshot. Final delivery/hub/PROVENANCE distinguish all current passes, historical failures/revisions and missing platform evidence. |
| 86 provenance/dependencies/evidence correspond to artifacts | COVERED for delivered local artifacts: current Linux/root/all-four-JDK receipts link snapshot 94b62848… to exact archived logs, products, five runtime JARs, Java image/version observations and explicit native installation. Hashes/source after all gates independently match. Earlier source/diagnostic/failure/success artifacts are preserved and labeled historical. Missing platform artifacts remain F1 and are not silently certified. |
| 87 inventory check/generated no drift | CURRENT PASS: actual ./scripts/inventory check exit 0 and BUILD SUCCESS at 2026-09-06T16:59:20Z, 21 capabilities/12 facade surfaces/20 exclusions, generated documentation current. Archived log matches its original and receipt SHA-256 b879e991a261e6fa2bb50281738b60822bc99eef4afe7cbf001a0ce3bd711b24 (1,537 bytes). Root verify also validates generated inventory. |
| 88 Standards no unresolved must-fix | The separate final independent Standards receipt records all local gates reviewed, no remaining mandatory source/archival finding and one nonblocking private-reference-row representation judgment. This Spec review does not replace that axis. Its final receipt retains the same missing-platform limits; no source must-fix from either axis remains unresolved. |
| 89 Spec full criterion coverage/no must-fix | Full criterion/constraint coverage is supplied by this independent map. F2/F3/F5/F6 source findings and F4 local verification are closed. F1 remains a mandatory unmet delivery requirement, so no whole-issue signoff or completion is supplied. |
| 90 diff check | CURRENT PASS: read-only fixed-baseline git diff --check returns 0, empty output. Recorded final workspace observation and independent review agree; later review-copy/link-receipt updates are documentation only. |
| 91 T29-only final workspace, no task temporaries | CURRENT reviewed scope: 42 changed tracked paths exactly match the scope receipt; 269 untracked at the independent final read differ from its earlier 268 only by addition of that workspace receipt itself. No unrelated path, staged entry or commit is present. Build/native cache is ignored; diagnostics, clones and heap dump remain under /tmp. Review-copy refresh preserves the same T29-only scope. |
| 92 per-criterion final report/commands/platforms/commit authority | T29-delivery.md maps every required observation, all actual validation commands/results, current/historical snapshots, the three platform gaps and uncommitted authorization. This independent complete Spec map supplies its final reviewed coverage. Parent copies the final review supplement and refreshes the existing workspace receipt; no new implementation or gate rerun is required. No commit is authorized; HEAD remains baseline. |

## Source boundary and lifecycle inspection

The C helper validates the outer bounded subprocess envelope and its inner HRQ1 payload, lengths, EOF, UTF-16, script/language/direction/glyph limit, source font/UPEM and compiled/runtime exact native version. It invokes HarfBuzz's public C OpenType font functions and shaper, outputs source GIDs/monotone UTF-16 clusters/independent positions, bounds returned glyphs and cleans up its allocations. No native implementation or font discovery is hidden behind Java dependencies. `ShapingRequest.java:97`/147 and `ShapingResult.java:57` are closed detached values; result decoding validates exact sizes, valid GIDs, scalar-boundary monotone clusters and reconstructs complete input ranges. Composition checks source GID/UPEM, horizontal advances and ICU grapheme boundaries again.

ShapingCoordinator enables composition only for an explicit request preference; registration alone leaves it off. Direct DrawPositionedUnicodeText always uses the prior unshaped preparation. Existing explicit source staging, embedding rights, full-font restrictions, budgets, unknown-resource preservation and publication remain in the established document boundary. Whole-grapheme fallback selects the first explicit font covering base plus marks. The admitted static TrueType extension adds exact axis reflection for complete unmodified Arabic fonts; it does not admit arbitrary fractional/shear composites, variable fonts, system fonts or new runtime bundles.

Paragraph shaping first obtains metrics, then reshapes every candidate at its actual original-grapheme line boundaries; ligature output does not replace original break boundaries. Native glyph order within clusters survives ICU visual ordering. Tabs/graphics remain atomic and existing overflow modes are preserved. Table intrinsic minimum computation considers legal partitions under the existing finite table work budget, including joined fragments narrower than isolated letters. Rejected candidate plans close; accepted plans transfer to paragraph/table scope through painting. Incremental tables retain required fonts/plans through flush/complete. The corrected F2 initial metrics scope now follows the enclosing layout/emission lifetime as well.

`PdfBoxShapedFont` adds actual source GIDs to the FontBox subsetter and emits a distinct document CID→embedded GID mapping, keeping required substitutions/composite dependencies even when cmap lacks them. Complete cluster ToUnicode and per-native-run ActualText are deliberate public observations; they do not promise reconstructed logical paragraph order across visual bidi runs or tagged-PDF structure. Deterministic distinct subset tags avoid both source/glyph collisions and existing document tags (`PdfBoxSubsetNames`).

Worker initialization carries only closed selected Provider metadata, never user code, native binaries or PDFBox objects. Original command/text/font/limit transport remains through WorkerCompositionCodec. Bounded shaping-required/result exchanges occur only during allowed command processing and invoke the selected Provider in the parent. Returned response reservations are released; the child's native result follows the same validation. This is explicitly not full native isolation by the Linux PDF Worker. JDK8 API/build enforcement and bundled-worker class inventories are retained.

The evidence recorder preserves separate native/install/semantic/syntax/subset/visual determinations, real OS/architecture/JDK/tool provenance, both-mode applicability and failure reasons. The Linux observer hashes installation/build source, actual helper and startup-mapped native engine files; malformed/tampered receipts or bytes fail, and ambiguous LD_PRELOAD/LD_AUDIT attribution remains indeterminate. It is a separate same-environment invocation, not a claim to trace every actual Provider process or certify other OSes.

## Constraints and execution discipline (C:33–42, 95–114, 117–140)

| Contract | Inspection result |
| --- | --- |
| 33–36 read context; profiles/version/fonts/input/geometry before product; independent expected source | Full authorities read. Frozen corpus/official hb-shape/raw PDF/fontTools chain reviewed. Oracle is separate from adapter/product. Retained reference red/green and authoring receipts support order; current source/generator/font/reference/raster hashes match. Required nonLinux acquisition remains missing F1. |
| 37–39 existing Provider/fonts/Composition/Workflow; continuous minimal tests | Implementation stays inside existing bounded contexts with small provider-contract DTOs and optional conversion adapter. Historical focused test logs show stepwise changes. No independent backend SPI or production dependency cycle. |
| 40–42 platform evidence/docs/inventories/gates/fixed-point reviews/delivery | Docs/inventories/all local gates and independent fixed-point reviews are delivered. Mandatory three-platform evidence remains F1, preventing whole-issue completion; no local pass fills those rows. |
| 95 offline/explicit font contract | No runtime network or system-font lookup; caller-provided fonts and helper only. Installer consumes a caller-local pinned archive, does not download. CI explicitly downloads build/test dependencies. |
| 96–97 preserve positioned unshaped; ICU not replacement shaping | Public regression uses a real but deliberately insufficient Provider limit to prove direct positioned and registration-only composition stay unshaped. Native helper supplies GSUB/GPOS; ICU provides segmentation/script/bidi. |
| 98–99 Provider/Worker honesty | Parent brokerage, original Worker envelope, native allocations not certified RSS/heap isolation; English/Chinese/matrix/evidence say so. |
| 100 no system/online font evidence | Complete unmodified hash-pinned Noto sources with OFL and explicit fallback declarations. No discovery path. |
| 101 clean-room/no iText source/resources/reverse closed code | PROVENANCE describes public standards/HarfBuzz C API, public fontTools/Meson/GCC/procfs references and project-authored code. No copied Reference Suite source/resources or closed-product output golden discovered. Parent issue reference names serve inventory context only. |
| 102 no lowered tolerances/dropped profiles/skipped platforms/looser resource defaults | All four/two-page profiles, exact font units, .0001 pt, primary AE0 and secondary3000 retained. Default resource values are unchanged; explicit fixtures select bounded budgets. Missing platforms are reported, not marked skipped/pass. |
| 103 report mandatory missing platform | F1, evidence table and matrix explicitly INDETERMINATE. |
| 104–108 scope/no remote writes/release/commit authorization | Worktree diff/untracked relevant to T29. HEAD=baseline, no commits. Reviewer has made no prohibited action; parent states no authorization. Final delivery must remain uncommitted. |
| 109–111 fixed base plus worktree/untracked review | Applied exactly; empty committed range is explicitly recorded above. |
| 112 every validation/generation seam strict Red→Green→Refactor | The original development receipt and all 125 then-listed log originals were independently audited. Native DTO/Provider, reference/generator/subsets, semantic/Worker, recorder, installer/observer and canonical-entry Red/Green/refactor sequences were read. PDF-version, relayout and fallback corrections include genuine failures and successful focused refactors. Latest snapshot canonicalization has a genuine owned-memory-policy Red, one-test Green and 58-test refactor; its earlier fixture-policy error is explicitly not the meaningful Red. F6 has an assertion Red, one-test Green and 58-test refactor. Both intermediate failures and successful correction receipts remain labeled honestly. Logs support observed discipline without recreating every historical source state. |
| 113 independent clean-context review before any checkbox; not implementer self-review | Parent spawned independent Standards and this full Spec reviewer. No criterion is marked satisfied. This reviewer did not implement or execute fixes. |
| 114 focused pass after each code-producing step | All source repairs/canonicalization/accounting have meaningful Red/Green/refactor receipts. Current full root, each JDK8/11/17/21, six-chain Linux acceptance and inventory pass with exact source/log correspondence. No post-freeze executable difference was observed; final narrative/review-copy refresh needs no repeated passing gate absent a new concern. |
| 117–140 issues/comments/context/ADRs/implementation/tests/own acceptance entry | Required context and relevant existing tests read. No #1 unrelated program expansion. T29 own recorder is bound into canonical `scripts/acceptance` plus dedicated T29-only Maven path; existing T28 results are not counted as T29 proof. |

## Reviewer read-only evidence checks

The reviewer inspected actual original red/green/refactor output, not filenames alone: native Provider and reference/seam red failures, semantic/product/recorder missing-seam reds, Worker wrong extracted order, and later public behavior regressions. The historical installer environment “green” filename contains a failed intermediate run and is correctly followed by a successful correction rather than treated as passing. PDF-version first “green” likewise still failed Worker transport; the later successful receipt is the one supporting closure.

Read-only hashing matched all 125 development-log originals to the historical record, four generated reference artifacts and both generation-source hashes to the reference receipt, all five used font hashes, and all eight frozen raster hashes to visual properties. At the original archival observation, Linux records were inspected with mode coverage and source/artifact linkage; all 663 input, 136 archive and four reactor JAR hashes/sizes matched. This predates the executable cache repair and does not describe current source/JAR equality. No native test/build or Maven was run by this reviewer. Final source-freeze updates to this map will record only newly observed facts and resolved findings.

Latest follow-up: `/tmp/folio-t29/shaping-final-review-refactor.log` finished at 2026-09-06T22:43:29+08:00 with all 34 shaping Workflow cases passing (zero failures/errors/skips), SHA-256 `2138a84ac3031a4c8fbe16ce775c7e649d190d58eb2a13f547587ae3579ac595`. After all three repairs, reviewer `git diff --check dd4cda28c24a304258e75f0854d86852275f5320 --` passed and HEAD still equals that baseline. Source defect review is closed for the inspected implementation. The later follow-up below resolves final source-linked Linux archival; mandatory nonLinux evidence and final root/JDK/inventory/review records remain open delivery items.


## Historical Linux archival follow-up (original snapshot, before cache repair)

The reviewer independently inspected the post-repair run and current source without executing Maven, native tools or product tests. The exact observed parent command is:

```sh
./mvnw -B -ntp -pl pdf-acceptance -am -Pacceptance-t29-record -DskipTests -Dacceptance.output=/tmp/folio-t29/evidence-linux-final verify
```

Its original log `/tmp/folio-t29/evidence-linux-final-reactor.log` reports BUILD SUCCESS, native/semantic/syntax/subsets/visual PASS, finished at 22:46:38+08:00, SHA-256 `18c8bfa9762cfcaf4306a3efa1a703415f7cfb0ae1ae2eb3c413923afdfe6bb7`. The separate installation chain is PASS in its retained record and actual observation JSON. This acceptance-only command deliberately skips unit tests and is not the required full root verify; the independent earlier 34-case public test refactor supplies the focused test evidence.

Read-only checks verified all 663 source/reference input file hashes and sizes, the canonical snapshot hash, snapshot-file hash in the run manifest, all 136 archive hashes/sizes and equality to original final-run files, all four current reactor JAR hashes/sizes, and the command log. Independent enumeration of all delivered module src trees, POMs/wrappers/config/scripts/CI/LICENSE/NOTICE/frozen profiles/rasters exactly matched the snapshot scope (no omitted or additional input). The snapshot excludes narrative docs/inventory/run receipts explicitly; their current changes were reviewed separately.

The creation receipt `/tmp/folio-t29/final-verification-snapshots.json` is timestamped 2026-09-06T14:50:03.352254Z. During the earlier Linux archival follow-up, before the later documentation updates, the reviewer independently compared both verification clones to the then-current original worktree: all 1,567 delivered-file bytes matched in `/tmp/folio-t29/matrix-8-11` and `/tmp/folio-t29/matrix-17-21`, and all 663 runtime snapshot hashes matched. This was a point-in-time observation, not a claim of continuing equality of all documentation. A fresh comparison at 2026-09-06T14:57:49Z found exactly three later differences in each clone: PROVENANCE.md, capabilities/evidence/artifacts/T29-development.txt and docs/zh-CN/getting-started.md. Their deltas are documentation/evidence excerpts and were read; all 663 frozen compilation/reference inputs still matched at that timestamp. These checks validate the input copies, not still-running gate results.

The native observation contains 99 exact glyph matches and no false match; reopened semantic output contains 198 exact item matches across both modes and no false result. Both subset-verifier outputs report PASS with source-glyph/CID/embedded-GID mappings. All six records explicitly identify Linux, JDK 17.0.9+9-jvmci-23.0-b22 and the pinned helper/reference/font provenance; the four product chains explicitly record both execution modes.

The reviewer independently opened all 16 PDFium/expected/secondary PNG triples, checked dimensions 480×384 and RGB pixels, and verified each copied expectation equals its frozen repository raster. Every primary changed-RGB-pixel count is zero. Secondary counts (equal in both modes) are Arabic 1261/402, Hebrew 1227/588, Devanagari 1437/1137, Thai 1001/565; maximum 1437 is below the original 3000 bound. This independently confirms the archived reported counts without rerendering or rewriting any image.

`capabilities/evidence/T29-shaping.md:70`–92 links the exact source/run manifests and all six chains and preserves overall INDETERMINATE, the absent three platforms and experimental status. `capability-matrix.yaml:2030`–2056 records only actual syntax/semantic/visual PASS and no certified platform. That earlier follow-up had final gates pending. The later read below verifies root and inventory PASS and accepts the documentation delta; the full four-JDK gate and final review archival remain unfinished. No new source defect, unsupported promotion or scope expansion was found.


## Historical receipt precision and planned Javadoc-only delta

As of the independent 2026-09-06T14:57:49Z read, the 663-file tested source snapshot remains unchanged. The exact three clone differences above add already completed regression/inventory log excerpts, correct the PROVENANCE delivery status and explain per-scalar fallback counting in Chinese. They do not alter executable inputs or profile tolerances. The earlier all-1,567-file match is retained solely as that earlier observation and the creation receipt’s statement.

A supplemental direct `javadoc -Werror -Xdoclint:all` invocation (`/tmp/folio-t29/final-public-javadoc.log`) initially reported 17 missing main descriptions on getters with @return-only comments: ShapingRequest.java:102–112 and ShapingResult.java:38–44,143–155. The repository release profile ordinarily disables doclint; this supplemental failure is a documentation follow-up under C:85, not evidence that the completed native/behavior run failed. Parent subsequently added conventional multiline summary/@return comments after root verify; the final source and debug-line delta review below supersedes this earlier pending status.

There is no Spec objection to preserving the original acceptance/gate receipts for such a comment-only change if a separate final-delta record preserves the original source/run manifests, identifies exact before/after source hashes and diffs, demonstrates that changes are only comments, records actual compilation and verifies all affected compiled class bytes—including nested classes/metadata—are identical to tested artifacts, and retains the successful strict Javadoc command/result. C:85 documentation and C:86 artifact correspondence would then be supported independently. Once Java comments change, the complete original 663 source-file hashes will no longer match the final working tree; the final report must explicitly distinguish the tested snapshot from the documented, verified comment-only delta. A claim that all final source hashes remain unchanged would be incorrect. No native rebuild, profile change or behavioral rerun is inferred to be necessary solely from verified comment-only changes with identical executable bytes; any non-comment or class-byte difference requires reassessing the affected validation.


## Historical Javadoc delta, debug metadata exception and completed root gate

The point-in-time read at 2026-09-06T15:31:15Z independently inspected `/tmp/folio-t29/final-getter-javadoc.patch`, `javadoc-before.json`, `final-javadoc-delta.json`, the preserved tested class bytes, current actual class bytes and all raw javap views. Exactly 17 getter comments in ShapingRequest.java and ShapingResult.java gain main summaries plus conventional multiline @return tags. Removing only `/** … */` comments leaves the two original/final sources byte-identical. At that observation, every source hash in the original 663-file snapshot matched except those two declared Javadoc files; both matrix clones matched all 663 original hashes. This predates the later executable cache repair and is not a current-worktree equality claim. The source patch SHA-256 is `224bc59eefe6cb2a8d8aa4b041d6d6b09b1f4dea89b00e85b6a70f5a1da01def`.

The documentation rebuild command `./mvnw -B -ntp -pl pdf-conversion -am -DskipTests package` passed at 2026-09-06T15:22:19Z. Its log SHA-256 is `4d4ec3f4f873fadf7a5e544db836222e3cf45d6d629e41cb574f83c3c1eac1b7`. The strict Javadoc receipt records `javadoc -quiet -Werror -Xdoclint:all --release 8` over ShapingRequest, ShapingResult and HarfBuzzCapabilityProvider with the provider/conversion compiled classpath, exit 0 and an empty warning/error log (SHA-256 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`). The 17-warning documentation issue is resolved. These commands were executed by the parent, not this reviewer; the reviewer only inspected receipts/files. The parent was asked to spell out the three source paths and record the rebuild log hash in the archived delta for exact command reproducibility.

The earlier suggested full class-byte identity proof is superseded by a narrower independently checked exception: multiline Javadocs move source/debug line numbers. The final delta correctly does not claim full class-byte identity. All 32 provider/conversion class entries (including nested classes) were independently enumerated and hashed against the before/delta receipts; no class was omitted or added. Twenty-nine are byte-identical. The remaining three are ShapingRequest.class, ShapingResult.class and ShapingResult$Glyph.class. The reviewer independently parsed their actual binary class files and masked only the two-byte `line_number` fields of each Code/LineNumberTable entry. Table counts, bytecode `start_pc` positions, constant pools, executable instructions, signatures, other Code attributes and every remaining byte were retained. All three pairs then matched exactly. Changed line-number entries were respectively 48 of 74, 58 of 64 and 18 of 18; class lengths remain respectively 5612, 5938 and 1955 bytes. Independent normalization of the retained complete javap -v -p views also reproduced all three recorded equality/hash claims after excluding only their file headers and LineNumberTable entries.

For that Javadoc-only follow-up, this was sufficient linkage for the existing native/behavior test evidence under C:85/C:86: no executable instruction, public signature, constant-pool value or other runtime metadata changed; only documentation and debug line numbers did. The strict documentation check validates the new Javadocs. Complete binary identity is not a user completion criterion, and the parent #1 explicitly excludes identical backend exceptions/byte-for-byte products from parity. Debug source locations must remain disclosed as a delta, rather than silently described as original byte-identical artifacts. Original native/acceptance/root/JDK receipts stay bound to tested source snapshot `39b761992f1677fe25ee7c4d5796c1505a024a890880d51aca95ed516024565f`; the separate source/class delta links the current documentation to it. No behavior/native rerun is required solely by this verified debug-line-only change, and no unresolved source defect arises from it.

The final root verification receipt and original log independently agree: explicit helper environment, `./mvnw -B -ntp verify`, actual exit 0, BUILD SUCCESS at 2026-09-06T15:06:41Z, 16:36 minutes. The log hash is `f37fa5baa8f2068bce42f21907f072dd8c131c4c1ab2b0e67d0972d9f7a1e2f6` (55,732 bytes). Summing all 58 per-class log records independently yields 1,018 tests, zero failures, zero errors and three skips solely in the existing HardenedWorkerScaleProfileTest. T29 native/public/acceptance tests and affected font/Unicode/paragraph/table/Worker regressions all ran in this root gate. The explicit inventory check passed at 15:13:43Z with 21 capabilities, 12 facade surfaces and 20 exclusions; its log hash is `d642d19dcfbde059ce5f11b99dc6da369a7f0377e2fb60d8b7ef1e254999bf1a`. Root verify also reports generated inventory documentation current. Current `git diff --check BASE --` passed after the Javadoc delta.

C:78 remains unfinished. The first complete JDK8 attempt (`final-matrix-8-11.log`) failed T28UnicodeEvidenceCommandTest.sevenProfilesMatchTheIndependentGlyphGeometryInBothExecutionModes with Worker transaction termination at 15:08:04Z. Its original log/XML remain preserved. The isolated same-JDK method passed at 15:11:14Z (one test, zero failures/errors/skips), but the complete rerun (`final-matrix-8-11-rerun.log`) again failed that method at 15:32:01Z. This is a real unresolved full-gate failure; an isolated pass does not replace the required complete JDK8/11/17/21 gate. The fact that the T28 test is unchanged does not by itself establish its cause or excuse the failure. No causal T29 implementation defect is inferred from these receipts alone. Missing Windows/macOS evidence remains F1 and is unaffected by any Linux/Javadoc/root success.

## Font-snapshot repair and declaration accounting follow-up (before refreshed acceptance)

This follow-up supersedes earlier references to a final/current source freeze. It is a source review plus inspection of parent-run diagnostics/tests; the reviewer ran no Maven/native/product command. C:76/C:78 require the affected font/Unicode regression and complete JDK gate; C:72–73/C:102 require preserving ownership, stable bounded failures, no pre-failure publication and unchanged defaults. The repair is within that authorized regression scope, without adding a new font-discovery, Provider, fallback or public API feature.

The two original full JDK8 failures were followed by diagnostics in clearly separate disposable clones. Preserved observations identify `java.lang.OutOfMemoryError: Java heap space` during FontBox CJK glyph loading, not merely a generic OS-kill message. The retained heap-array identity report matches full pinned payload SHA-256 values; the jhat observation identifies 19 one-shot declarations, including 11 complete CJK arrays. The layout-liveness-only probe still failed; snapshot-content sharing alone passed a narrowed 41-case acceptance run. The modeled Worker peak fell from 1,449,636,850 to 1,294,493,250 bytes in that prototype; this excludes the later declaration-metadata supplement and does not measure RSS. A fixed-baseline narrowed run passed once, but this does not establish that T29 introduced the pre-existing identity cache. Diagnostic logging, heap-dump flags and any prototype edits remain outside product source. Defaults, collectors, profiles and tolerances are unchanged.

Source inspection against the frozen matrix clone found only the targeted executable cache delta in `PdfBoxPositionedTextOperations` plus its public regression. `stagedOneShotSources` still keys each borrowed declaration by identity (lines 70–71). The new session-scoped content map at 73–75 shares only privately staged bytes. STREAM/CHANNEL reads remain bounded and checkpointed (`stage` 856–876, `read` 898–914), each distinct declaration is read once and the caller handle remains open. BYTES/PATH observation behavior is unchanged (835–852). `stageAndParse` still adds every complete staged length, including duplicates, to per-command source-byte accounting (788–796); all declarations still pass the existing format/embedding admission path. Hashing is checkpointed every 8,192 bytes and full length/byte equality at 1692–1712 prevents a hash collision from merging different programs. The new borrowed-key constructor does not change close ownership of parsed font keys; temporary staged-copy accounting closes after the canonical snapshot receives its retained charge. Unique content/key storage is charged at 869 and every first declaration entry at 874, before insertion; same-declaration reuse creates neither a new entry nor an extra retained charge. Failure remains terminal through the existing resource context and cannot publish a partial target.

The public `FontLoadingWorkflowTest.equalBorrowedDeclarationsKeepRepeatedFontUseWithinABoundedBudget` (320–389) compares 128 positioned-text commands using one reused source against 128 distinct alternating borrowed stream/channel declarations. It copies all existing safe policy fields and gives the second run only 65,536 bytes above the public control peak. It requires COMMITTED publication, bounded peak use, a larger peak for distinct declarations, exactly one read of each borrowed payload, open borrowed handles, and a reopened embedded font/ToUnicode mapping. The primary assertions use public Workflow/queries and observable usage; none depends on private map identity or a fixed internal accounting formula. The new test is IN_PROCESS; the same focused suite additionally covers real Worker shaping and the T28 transaction, and full existing Worker/matrix gates remain required.

The canonicalization meaningful Red is `font-snapshot-canonicalization-policy-red.log` (owned-memory failure, exit 1); its initial `-red.log` only caught an incomplete policy fixture and is explicitly not accepted as Red proof. Minimal Green passed one case, followed by 58 focused cases. For F6, `font-declaration-accounting-red.log` records one assertion failure at 2026-09-07T00:32:04+08:00, SHA-256 `cbe3287e5e648737cace36321caaa36406fbda92a2c0a984c888083171f11ed0`; Green passed one case at 00:32:28, SHA-256 `911b946e04f3d1cce71efeb24ed4439582a06b6dc9e11d10c8ee60594f07930b`. The final `font-declaration-accounting-refactor.log` finished BUILD SUCCESS at 00:36:03 with independently summed 58 cases (22 FontLoading + 34 shaping + 2 T28), zero failures/errors/skips, SHA-256 `1a56928fbda9b0169eecbca64f08937b214d581be313248ea9b6394ab43df8e1` (14,140 bytes). That run reports T28 Worker modeled peak 1,294,495,682 after the metadata supplement. These focused passes close the source/accounting finding; they do not substitute for complete gates or required nonLinux execution.

The reviewer rehashed all 12 logs plus the font-array and cache-owner observations listed in `capabilities/evidence/artifacts/T29-font-cache-repair.json`: all 14 archived file sizes/SHA-256 values match. The archive explicitly distinguishes diagnostics, baseline observations, the invalid initial Red, genuine Red/Green/refactor runs and prototype-only numbers. The later final declaration refactor may be added by the parent after this receipt observation. `T29-linux-before-font-cache-repair.zip` SHA-256 `651865271754e8c09f9e7eb6dfa860139659ac22643e11eb49a8ed5afbea02a9` contains 138 entries: all 136 original run artifacts plus the original source and run manifests. Independent ZIP/entry hashing found no mismatch; the ZIP run manifest equals the separately embedded original manifest and the preserved source-manifest hash equals the original run reference. Historical root and JDK11/17/21 successes remain valid observations on their original snapshot, but neither those nor the earlier verified Javadoc debug-line exception establishes correctness of this executable repair.

Original complete JDK11 passed at 2026-09-06T16:00:01Z, log SHA-256 `bfc7300330e35bcf2c0ad616b973360cef46ec89e83660dfab885cd9df7e082d`; JDK17/21 passed at 15:24:37Z/15:40:52Z, combined log SHA-256 `9d2afb7025722f2351f62477efe7be2f276d7f35b89901070895fce1a892c7dd`. Per-class totals independently confirm 1,018/0/0/3 for each. They are historical and do not waive the twice-failed original JDK8 gate or required fresh post-repair runs. At this follow-up HEAD still equals the fixed baseline, the committed range is empty and read-only `git diff --check BASE --` passes. Fresh final source snapshot, Linux acceptance, full root/JDK matrix, inventory and final documentation/receipt correspondence are pending F4. Missing Windows x86-64, macOS x86-64 and macOS arm64 remain F1 INDETERMINATE. No criterion is marked complete and no commit is authorized.

## Current Linux acceptance and archival follow-up (snapshot 94b62848…)

The parent-run current-source command completed with actual exit 0 and BUILD SUCCESS at 2026-09-06T16:38:23Z in 1:25 minutes:

```sh
FOLIO_HARFBUZZ_HELPER=/home/ubuntu/IdeaProjects/open-pdf/.build-cache/harfbuzz/10.2.0-final/bin/folio-harfbuzz \
FOLIO_SHAPING_PYTHON=/usr/bin/python3 PYTHONPATH=/tmp/folio-t28/python \
./mvnw -B -ntp -pl pdf-acceptance -am -Pacceptance-t29-record \
-DskipTests -Dacceptance.output=/tmp/folio-t29/evidence-linux-font-cache-repair verify
```

`capabilities/evidence/artifacts/T29-linux-run.json` records the exact command/environment, exit code, four reactor JARs, all 136 archived outputs and raw reactor log. The archived log `T29-verified-linux-reactor.txt` is 8,278 bytes, SHA-256 `118bbed0cab19fd3dcb52652e58b606c31699d19d19ef1d3a6f9edcc3d073bf4`. It records native/semantic/syntax/subsets/visual PASS; the separately retained installation chain is PASS. The command intentionally skips tests, so it is not the mandatory full root verify or JDK matrix. Fresh root/JDK gates remain pending F4.

Read-only hashing at 2026-09-06T16:42:24Z independently confirmed all 663 current input sizes/hashes and canonical SHA-256 `94b628481751378665a40a3b033430b1224ec0adc817f811386bc44db59ef24f`; source-manifest file SHA-256 `6521fd111a3f69bec58ec024ace436e4c48f0a827fb2599142c5000bf541751a` (139,143 bytes) matches the run manifest. Independent enumeration of the declared source/build/script/profile/raster scope produced exactly 663 paths, with none omitted or extra. Comparing original/current manifests found exactly the four declared changed inputs: PdfBoxPositionedTextOperations, FontLoadingWorkflowTest, ShapingRequest and ShapingResult. All reference/native/build inputs apart from those Java code/test/comment changes remain unchanged. Every one of the 136 archived files has the recorded size/SHA-256 and equals the corresponding file in the actual output directory. At the same read, all four current reactor JAR sizes/hashes match the run manifest. These are point-in-time artifact observations; later gate output must remain separately bound to its run/source identity.

The 80 current PNGs were independently compared byte-for-byte with the preserved prior ZIP; all match. This carries forward the already inspected images and independently computed 16-page comparison observations: primary zero changed RGB pixels on every page in both modes, secondary maximum 1,437 below the original 3,000 limit. The current PDFs were independently compared with both prior products using the recorder's exact ID-neutral policy: replace only each of the two hexadecimal trailer `/ID` values with ASCII zeroes of the same length. Each PDF contains exactly one matching ID array. An independent byte-position check verifies that only those two value spans change; lengths, all surrounding syntax, streams and every other byte remain intact. All four normalized PDFs then compare exactly and hash `a6c1522148d25ec716119791bdc682b9c3b7682b0b9ebfcbbc69a8823964318c`. An earlier draft comparison replaced the whole ID array with shorter placeholders; the parent superseded that supplementary normalization with this exact existing recorder policy. No original PDF, PNG or generated run artifact was changed by that receipt precision correction.

The existing native startup observation is timestamped 2026-09-06T13:57:45.613005Z and remains a separate earlier same-installation observation, not a claim to trace every Provider process in the new acceptance run. Its actual helper hash `169389e19e28bc96e3e878a9468671c31ccc6d5e7abbc3d38c4b8470526cdc65`, mapped engine hash `8a4c67dc9ead9b0752bcf256fa46c39eb3592e403a282bb341637ed989196e10`, installation-receipt hash and observer-source hash were rechecked against current files and match; the observer records an empty loader-override environment. Native metrics were actually rerun through the external Provider. This supports the explicitly described Linux installation/loaded-engine chain without asserting full Worker isolation or nonLinux compatibility.

The fresh clone receipt is timestamped 2026-09-06T16:37:34.063405Z. At 16:43:02Z, the reviewer independently enumerated all delivered files in both `/tmp/folio-t29/matrix-font-cache-8-11` and `matrix-font-cache-17-21`: each has 1,609 files and reproduces creation digest `14e63c5e45c1572dd959dbf793a1a99aacda12bffc42650c639f538206fd8b06`; all 663 current source-input hashes match in each. Eleven later original-worktree changes concern only PROVENANCE, delivery/hub/chain records, the run manifest, installation observation text and regenerated product PDFs outside the frozen compilation/reference input scope. Therefore the creation-time delivered-byte equality is supported and correctly qualified; continuing equality of all current narrative/evidence bytes is not claimed. Current root/JDK results were not inferred from those clone-copy checks.

The refreshed delivery receipt (35–66, 72–87, 93–110, 185–221) and evidence hub (55–70, 74–106, 126–141) preserve all six current Linux chains, exact snapshot/artifact links, original fixed tolerances, experimental status, no certified platforms, missing three platform receipts and the current full-gate limits. They distinguish original 39b76199… verification, the intermediate Javadoc-only revision and current 94b62848… executable repair. PROVENANCE documents the cache diagnosis/repair with its causal limits and no copied code/default changes. Small wording precision follow-ups were sent to the parent: describe the original three findings as initial findings when separately documenting later F6; state that the 58-case suite covers both modes rather than implying every FontLoading test is parameterized; update the now-completed Linux acceptance wording in PROVENANCE. A subsequent read verified all three precision corrections: hub:127 and delivery:112 say initial findings; delivery:180 says the suite passes and covers both modes; PROVENANCE:1779–1780 says current Linux acceptance passes six chains while root/four-JDK gates remain in progress. These follow-ups are closed and reveal no new implementation defect.

C:59 and the current Linux artifact-linkage portion of C:86 are now covered. C:77/C:78 full current-source root and JDK 8/11/17/21 verification remain pending, with no earlier pass substituted. F1 Windows x86-64/macOS x86-64/macOS arm64 remain INDETERMINATE under C:60–62/C:79–80. The reviewer ran no build, Maven test, native helper or recorder and made no repository mutation. Only this review map was updated. Read-only `git diff --check BASE --` passed again. No completion checkbox or whole-issue signoff is authorized.

## Current full root, JDK 8, runtime artifacts and inventory follow-up

This later read supersedes earlier pending root/JDK8/inventory statuses. Contract C:77 requires the complete root command, C:78 separately requires all four JDKs, C:86 requires evidence/artifact correspondence and C:87 requires a current inventory check. The reviewer inspected completed parent-run logs and archives only, without running Maven, native code, a container or an inventory command.

`T29-verified-root-verification.json` records the exact helper environment and `./mvnw -B -ntp verify` in the original worktree, actual exit 0, BUILD SUCCESS at 2026-09-06T16:57:03Z and 16:38 minutes. Archived `T29-verified-root-verify.txt` equals `/tmp/folio-t29/verified-root-verify.log`, SHA-256 `75d5350e31f8c36625aedbe5c597207d3692b1914722c8dc97758495f102b55e` (55,053 bytes). Independently summing all 58 per-class records gives 1,019 tests, zero failures, zero errors and three skips solely in the existing `HardenedWorkerScaleProfileTest`. The additional test relative to the earlier 1,018-case run is the public borrowed-font cache/accounting regression. The full run includes four HarfBuzzCapabilityProvider, 22 FontLoading, 34 ShapingComposition, two T28UnicodeEvidenceCommand and five T29ShapingEvidenceCommand cases plus all other repository classes. T28 reports IN_PROCESS modeled peak 1,207,232,298 and HARDENED_WORKER 1,294,495,682, with both public acceptance cases passing. These are modeled owned-byte peaks, not actual RSS. The archived root environment also hash-matches its receipt and reports Linux x86-64, GraalVM/OpenJDK 17.0.9 and Maven 3.9.16.

`T29-verified-jdk8.json` records the completed JDK8 portion of `./scripts/verify-jdk-matrix.sh 8 11` in `/tmp/folio-t29/matrix-font-cache-8-11`: BUILD SUCCESS at 16:56:11Z, 18:16 minutes and independently counted 1,019/0/0/3 with the same affected classes and T28 peaks. The 67,214-byte `T29-verified-jdk8.txt` archive, SHA-256 `e62012ee722edc0ff663d033a3c6dc943d6db47c9661cfe2ff3a642f469ea860`, equals the exact first block of the original combined `/tmp/folio-t29/verified-matrix-8-11.log` before the JDK11 banner. The matrix script has `set -euo pipefail` and directly awaits each Podman/Maven invocation; advancement to JDK11 therefore supports JDK8's successful return without falsely claiming the enclosing 8/11 process has finished. The actual pre-run image observation identifies Linux amd64 Temurin8 image ID `abe8c22c734bcff3c2e9ec5f8549f9aeb9a27aabc7b53974ddadcc903d30644a`; its retained version observation is 1.8.0_502-b07. Both observations match the JDK8 receipt. The older failed full JDK8 attempts and their diagnosis remain archived; a current complete pass now closes the JDK8 portion on repaired source.

At 2026-09-06T17:03:23Z the reviewer rehashed all 663 current snapshot inputs in the original worktree and both fresh matrix clones. Every size/hash still matches current canonical snapshot `94b628481751378665a40a3b033430b1224ec0adc817f811386bc44db59ef24f`; no executable source/default/profile differences from the frozen inputs were observed after the current acceptance/root/JDK8 runs. This establishes source correspondence, not the results of still-running JDK gates. Current JDK11 and JDK17 were in progress at this review, with JDK21 to run separately in whichever clone frees first. Actual command grouping is `8 11`, `17`, then `21`; the earlier planned `17 21` grouping is not falsely reported as executed. Whole C:78 remains pending.

The five actual runtime JARs independently match `T29-verified-runtime-jar-inspection.json` and the root receipt: provider-contract, conversion, document, stable migration and preview migration. Every one of their 744 non-directory entries was read. Independent binary-signature checks cover ELF, PE and Mach-O (with Java class magic distinguished from fat Mach-O); filename checks additionally cover native suffixes, versioned .so names and helper basenames. No native binary/engine/helper entry is present. Known unofficial wrapper prefixes also have no entry. This scan supports C:63/C:64 together with the earlier full source/POM/dependency review; it is not represented as an exhaustive audit by package-name search alone. The receipt explicitly states that scope. Runtime JAR inspection does not treat separate repository acceptance fonts/native build cache as product bundles.

`T29-verified-inventory-check.json` records actual `./scripts/inventory check` exit 0. Its archived raw log matches `/tmp/folio-t29/verified-inventory-check.log`, SHA-256 `b879e991a261e6fa2bb50281738b60822bc99eef4afe7cbf001a0ce3bd711b24` (1,537 bytes), and reports BUILD SUCCESS at 16:59:20Z, 21 capabilities, 12 facade surfaces, 20 exclusions and generated inventory documentation current. C:87 is covered on the current delivery authority. The current root gate also passed inventory validation. Read-only `git diff --check BASE --` still passes.

Updated delivery rows 72 and 93–105 and hub 55/126–138 distinguish current root/JDK8 successes from pending JDK11/17/21 and missing Windows/macOS evidence. PROVENANCE likewise states the completed current local gates while retaining causal limits for the original cache failure. The JDK8 receipt's earlier point-in-time scope still mentions root pending; its timestamp precedes archival of root completion and it is superseded by the current root receipt and delivery text, not silently reused as a current whole-gate status. No new source defect, evidence mismatch, profile relaxation or scope expansion was found. F4 now concerns remaining current JDK11/17/21 gates and final supplemental receipts; F1 still prevents #30 completion regardless of their future result. No checkbox, commit or issue completion is authorized by these observations.

## Final complete matrix, source/image/log linkage and workspace closure

This final narrow read-only review closes F4 and supersedes all earlier pending local-gate statuses. The original contract still controls: C:78 requires all four JDKs to pass; C:86 requires evidence/artifacts to correspond; C:90–92 require fixed-baseline diff/workspace/report precision. No passing gate was rerun by this reviewer. Only completed parent-run receipts/logs/source files were inspected; only this `/tmp` map was written.

`capabilities/evidence/artifacts/T29-verified-jdk-matrix.json` records all four actual Linux gates PASS on current source identity `94b628481751378665a40a3b033430b1224ec0adc817f811386bc44db59ef24f`. All linked source/image/command receipts independently match their declared hashes and sizes. Each full raw matrix log archive is byte-identical to its original `/tmp/folio-t29/verified-matrix-*.log`. Independent per-JDK parsing confirms exactly 58 class records and 1,019 tests / zero failures / zero errors / three skips; only `HardenedWorkerScaleProfileTest` skips. Each block has one BUILD SUCCESS and no BUILD FAILURE. The repaired full T28 Worker trigger passes in every JDK with modeled peak 1,294,495,682; no native-RSS guarantee is inferred.

| Actual JDK gate | Finished UTC, 2026-09-06 | Duration | Exact archived log SHA-256 |
| --- | --- | --- | --- |
| 8 | 16:56:11Z | 18:16 | 8/11 combined `d727830f0292af38dcfe3b7d568acb514a0e54ea64a08ebd6beeddcbf1e83ca4` (132,314 bytes) |
| 11 | 17:14:22Z | 18:08 | Same exact combined log; separate JDK11 block independently counted |
| 17 | 17:15:26Z | 17:28 | `51982869ca142a8cdca32bf275079e118b04a64cd8eec0f687678c3b4db6eb98` (67,363 bytes) |
| 21 | 17:29:27Z | 14:16 | `8c490e20356f9d766ab282a763dbbe281791899ec2a11ecfd314e5313b539783` (65,186 bytes) |

The exact enclosing command receipts record actual exit 0 for `./scripts/verify-jdk-matrix.sh 8 11` in `matrix-font-cache-8-11`, `./scripts/verify-jdk-matrix.sh 17` in `matrix-font-cache-17-21`, and `./scripts/verify-jdk-matrix.sh 21` in the first clone after its 8/11 command ended, each with the declared explicit helper environment. The final grouping is recorded accurately; an unexecuted grouped `17 21` command is not substituted. The existing script's sequential set-e execution and complete per-block Maven success corroborate the recorded enclosing command exits. The historical single JDK8-block receipt's pending 8/11 scope remains a timestamped earlier observation and is superseded by these complete command/aggregate receipts.

Image observations cover before 16:37:54.792515Z and after 17:32:07.844009Z. Independent comparisons match each pre-run image ID, post-run image ID and aggregate JDK result; the recorded `java -version` command addresses the exact immutable image ID, exits 0 and agrees with the aggregate output. Versions are Temurin 1.8.0_502-b07, 11.0.32+9, 17.0.20+8 and 21.0.12+8-LTS, all Linux amd64. The reviewer executed no container or Java process for this check. At 17:39:07Z, all 663 input file hashes/sizes in the worktree and both verification clones still match the frozen source snapshot. This supports the final current-source matrix without extending image or execution claims to another OS.

The current root result, previously independently checked, is 1,019/0/0/3 with actual exit 0 at 16:57:03Z; current six-chain Linux acceptance, actual glyph subsets, 80 unchanged PNGs/ID-only PDF equivalence, five runtime JAR content checks and inventory PASS remain supported by the prior linked reviews. Their source inputs have not diverged from this final matrix snapshot. All original failed JDK8 runs, diagnostic/prototype sequences, meaningful Red/Green/refactors, original source/run ZIP and intermediate Javadoc/LineNumberTable-only proof stay preserved as historical evidence. Current successful complete gates are not used to erase prior failures or overstate their cause.

The final workspace observation at 17:36:26Z recorded 42 changed tracked files and 268 relevant untracked files before adding its own JSON. Independent `git diff BASE --name-only`/`git ls-files --others --exclude-standard` at 17:40:20Z finds the same 42 tracked paths and 269 untracked: the sole addition is `T29-verified-workspace-check.json` itself, with no removal or unexpected path. HEAD still equals `dd4cda28c24a304258e75f0854d86852275f5320`, the index is empty, commit count since baseline is zero, and fixed-baseline diff check returns exit 0 with empty output. The source snapshot and reviewed final delta contain no diagnostic instrumentation or relaxed defaults. Parent's marker scan reports no match in product/test/script roots; diagnostic-only data in retained logs and /tmp are labeled explicitly.

The workspace receipt records 755 inline local links across 46 changed/relevant untracked Markdown files with none missing at its timestamp. This reviewer independently resolves 758 inline local links across the same 46 documents at the later read, also with none missing; the later delivery/workspace links and parser scope explain why the earlier numerical count is not treated as an invariant. Final review-copy and existing workspace-receipt refresh may update that count without changing source or requiring another gate. Relevant changed/untracked source, native/reference inputs, fixtures, profiles, inventory, documentation and evidence remain within #30's delivery scope.

The fresh `T29-verified-platform-availability.json` records 17:26:09.976913Z Linux x86-64 availability, absent local Windows/macOS execution tools, a successful repository self-hosted runner query with zero runners, and no supplied authorized nonLinux endpoint. It explicitly does not infer that all GitHub-hosted or external platform services are globally unavailable. No push or new hosted workflow execution was authorized by this delivery contract. Thus the actual Windows x86-64, macOS x86-64 and macOS arm64 native/IN_PROCESS receipts still do not exist. These are concrete unmet execution-evidence requirements, not inferred shaping failures or a Linux-only waiver.

The final delivery receipt (3–7, 72–110, 121–164, 199–236), hub (55–70, 126–140) and PROVENANCE (1778–1784) consistently state all current local gates PASS while preserving three platform INDETERMINATE results and #30 unfinished. Per-JDK counts are stated as 1,019 for root and each of the four JDK gates; they are not an aggregate total for the entire matrix. Exact current/historical source identities, comment-only debug exception, executable cache repair, both clone creation-time byte comparisons, parent-side native boundary, experimental matrix status and absent certified platforms remain distinct and accurate. Separate independent Standards review reports no mandatory source/archival finding; its nonblocking private reference-row representation judgment does not change this Spec disposition.

**Final Spec disposition:** F2/F3/F5/F6 repaired source defects are closed; F4 local verification/artifact correspondence is closed; no additional source defect, unasked feature, scope expansion or wrong implemented-looking requirement was found. **F1 alone remains mandatory and unresolved:** C:60–62 require “完整、可追溯的固定版本实测证据” for the three absent platforms, and C:79–80 requires “INDETERMINATE，不能视为 #30 完成.” This full map covers every #30/contract criterion without satisfying or waiving those platform requirements. No whole-issue completion, checkbox or commit authorization is given. Final review-copy/workspace-receipt refresh is documentation/evidence bookkeeping, not outstanding product implementation or a reason to repeat passing gates.
