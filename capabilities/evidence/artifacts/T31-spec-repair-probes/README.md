# Independent Spec repair probes

These are the unchanged original public Workflow probes rerun by the independent Spec reviewer after both RAW repairs. `manifest.json` records commands, source/class identities, outputs and artifact hashes. Original pre-repair observations remain in `../T31-initial-spec-probes.txt`.

Rejected probes deliberately retain a three-byte sentinel target; their `.pdf` filenames do not indicate successful PDF publication. The stdout records distinguish COMMITTED products from REJECTED, retained targets. Final T31 recording separately retains the expanded actual PDFium corpus.
