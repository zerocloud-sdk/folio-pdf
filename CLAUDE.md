## Agent skills

### Issue tracker

Issues are tracked in GitHub Issues for `zerocloud-sdk/open-pdf`. See `docs/agents/issue-tracker.md`.

### Domain docs

Domain documentation uses a single-context layout rooted at `CONTEXT.md`, with architectural decisions in `docs/adr/`. See `docs/agents/domain.md`.
