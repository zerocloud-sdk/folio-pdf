<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>net.zerocloud</groupId>
    <artifactId>pdf-parent</artifactId>
    <version>${revision}</version>
    <relativePath>../pom.xml</relativePath>
  </parent>

  <artifactId>pdf-acceptance</artifactId>
  <packaging>jar</packaging>

  <name>Folio PDF by ZeroCloud - Acceptance Evidence</name>
  <description>Repository-only independent Acceptance Evidence pipeline.</description>

  <properties>
    <maven.install.skip>true</maven.install.skip>
    <maven.deploy.skip>true</maven.deploy.skip>
  </properties>

  <dependencies>
    <dependency>
      <groupId>net.zerocloud</groupId>
      <artifactId>pdf-document</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.apache.pdfbox</groupId>
      <artifactId>pdfbox</artifactId>
      <version>${pdfbox.version}</version>
    </dependency>
    <dependency>
      <groupId>com.twelvemonkeys.imageio</groupId>
      <artifactId>imageio-tiff</artifactId>
      <version>${twelvemonkeys.version}</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <systemPropertyVariables>
            <repositoryRoot>${maven.multiModuleProjectDirectory}</repositoryRoot>
          </systemPropertyVariables>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>acceptance-record</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>exec-maven-plugin</artifactId>
            <executions>
              <execution>
                <id>record-acceptance-evidence</id>
                <phase>verify</phase>
                <goals>
                  <goal>java</goal>
                </goals>
                <configuration>
                  <mainClass>net.zerocloud.pdf.acceptance.AcceptanceEvidenceCommand</mainClass>
                  <classpathScope>runtime</classpathScope>
                  <arguments>
                    <argument>${acceptance.output}</argument>
                    <argument>${maven.multiModuleProjectDirectory}/scripts/qpdf-pin.properties</argument>
                    <argument>${maven.multiModuleProjectDirectory}/scripts/pdfium-pin.properties</argument>
                    <argument>${maven.multiModuleProjectDirectory}/scripts/imagemagick-pin.properties</argument>
                    <argument>${maven.multiModuleProjectDirectory}/capabilities/profiles/T03-document-blank-visual.properties</argument>
                    <argument>${maven.multiModuleProjectDirectory}/capabilities/profiles/T18-canvas-images-colors-transparency-visual.properties</argument>
                    <argument>${maven.multiModuleProjectDirectory}/capabilities/profiles/T19-font-loading-embedding-subsetting-visual.properties</argument>
                    <argument>${project.version}</argument>
                  </arguments>
                </configuration>
              </execution>
              <execution>
                <id>record-rendering-evidence</id>
                <phase>verify</phase>
                <goals><goal>java</goal></goals>
                <configuration>
                  <mainClass>net.zerocloud.pdf.acceptance.T23RenderingEvidenceCommand</mainClass>
                  <classpathScope>runtime</classpathScope>
                  <arguments>
                    <argument>${acceptance.output}</argument>
                    <argument>${maven.multiModuleProjectDirectory}/scripts/qpdf-pin.properties</argument>
                    <argument>${maven.multiModuleProjectDirectory}/scripts/pdfium-pin.properties</argument>
                    <argument>${maven.multiModuleProjectDirectory}/scripts/imagemagick-pin.properties</argument>
                    <argument>${maven.multiModuleProjectDirectory}/capabilities/profiles</argument>
                    <argument>${project.version}</argument>
                  </arguments>
                </configuration>
              </execution>
              <execution>
                <id>record-paragraph-evidence</id>
                <phase>verify</phase>
                <goals><goal>java</goal></goals>
                <configuration>
                  <mainClass>net.zerocloud.pdf.acceptance.T24ParagraphEvidenceCommand</mainClass>
                  <classpathScope>runtime</classpathScope>
                  <arguments>
                    <argument>${acceptance.output}</argument>
                    <argument>${maven.multiModuleProjectDirectory}/scripts/qpdf-pin.properties</argument>
                    <argument>${maven.multiModuleProjectDirectory}/scripts/pdfium-pin.properties</argument>
                    <argument>${maven.multiModuleProjectDirectory}/scripts/imagemagick-pin.properties</argument>
                    <argument>${maven.multiModuleProjectDirectory}/capabilities/profiles</argument>
                    <argument>${project.version}</argument>
                  </arguments>
                </configuration>
              </execution>
              <execution>
                <id>record-paragraph-pagination-evidence</id>
                <phase>verify</phase>
                <goals><goal>java</goal></goals>
                <configuration>
                  <mainClass>net.zerocloud.pdf.acceptance.T25ParagraphEvidenceCommand</mainClass>
                  <classpathScope>runtime</classpathScope>
                  <arguments>
                    <argument>${acceptance.output}</argument>
                    <argument>${maven.multiModuleProjectDirectory}/scripts/qpdf-pin.properties</argument>
                    <argument>${maven.multiModuleProjectDirectory}/scripts/pdfium-pin.properties</argument>
                    <argument>${maven.multiModuleProjectDirectory}/scripts/imagemagick-pin.properties</argument>
                    <argument>${maven.multiModuleProjectDirectory}/capabilities/profiles</argument>
                    <argument>${project.version}</argument>
                  </arguments>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
